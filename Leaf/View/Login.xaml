<Page
    x:Class="Leaf.View.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leaf.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <Binding Path="Login" Source="{StaticResource Locator}"></Binding>
    </Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="1"  Grid.Column="1"  Header="用户名" MinWidth="300" Text="{Binding Username,Mode=TwoWay}" />
        <PasswordBox Grid.Row="2" Grid.Column="1"  Header="密码" Password="{Binding Password,Mode=TwoWay}" />
        <Button Grid.Row="3" Grid.Column="1" Margin="0,20,0,0" HorizontalAlignment="Stretch" Content="登陆" Command="{Binding LoginCommand}" />
        <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,10,0,0" Orientation="Vertical">
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="注册" Command="{Binding ToRegister}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="测试习题" Command="{Binding ToSingle}" />
        </StackPanel>
    </Grid>
</Page>
