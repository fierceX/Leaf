<Page
    x:Class="Leaf.View.test"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leaf.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <Binding Path="Test" Source="{StaticResource Locator}"></Binding>
    </Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Name="ButtonStackPanel2" Orientation="Vertical">
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="测试解析JSON" Command="{Binding ToJson}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="试卷管理" Command="{Binding ToTest}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="题目列表" Command="{Binding ToQuestion}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="下载" Command="{Binding ToStore}" />
        </StackPanel>
    </Grid>
</Page>
