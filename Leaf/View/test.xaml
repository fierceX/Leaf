<Page
    x:Class="Leaf.View.test"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leaf.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <Binding Path="Test" Source="{StaticResource Locator}"></Binding>
    </Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Name="ButtonStackPanel2" Orientation="Vertical">
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="测试单选题" Command="{Binding ToSingle}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="测试填空题" Command="{Binding ToGap}" />
            <Button Margin="0,10,0,0"  HorizontalAlignment="Stretch" Content="测试解析JSON" Command="{Binding ToJson}" />
        </StackPanel>
    </Grid>
</Page>
